# NestJS API

To test this project simply run:

```shell
docker compose up dev-db -d
```

Prisma commands:

```shell
npx prisma migrate dev
```

```shell
npx prisma generate
```

```shell
npx prisma studio
```
